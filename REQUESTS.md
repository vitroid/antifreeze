# 次の改良

- [x] 画面右サイドに全文プレビュー（本文 Markdown を `marked` + `DOMPurify` で表示。編集に追従）
- [x] すでに存在するFrontMatterのすべてを編集したい。(新たに追加しなくてもいい)
- 一行preview (現在入力している行をリアルタイムでプレビュー) ※不要のため見送り
- [x] 階層型ファイルブラウザ（基本版）
  - [x] ".\_"ではじまるファイル名は表示しない。
  - [x] 拡張子md以外は表示しない。
  - [x] ファイルを選ぶと、pathに'/../../"が付いて、ファイルが開けない。
  - [x] サブディレクトリのファイル名が空で選べない (404 Not Found)
  - [x] ファイルを選ぶと、pathに'/chemweb/"が付いて、ファイルが開けない。
- [x] 保存などの重要ボタンはスクロールせず画面の定位置にあるべき。
- 新しい記事を作る方法
  - 「別名で保存」が一番安全
  - ファイルを消す機能もないと困るかも。
  - だから、「保存」「別名で保存」「削除」の3ボタンを並べる?それで過不足ないか?

# 将来計画

(まだ実装しなくていいです)

- Goldmark の設定に対応した拡張（属性・脚注等）
- 数式（KaTeX/MathJax）
- date枠がでかい。
- dateはJSTに統一。できれば日付用のUIを使ってほしい。
